import MainLayout from '@/components/layouts/Main';
import AreaContainer from '@/components/containers/Area';

const Area = () => {
   return (
      <MainLayout>
         <AreaContainer />
      </MainLayout>
   );
};

export default Area;
